info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/auth/login
  body:
    type: json
    data: |-
      {
        "email": "prasanth@prasanth-forge.com",
        "password": "secret123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
