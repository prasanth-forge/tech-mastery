info:
  name: GET one
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/users/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
