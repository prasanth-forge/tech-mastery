spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///Users/prash/lab/tech-mastery
          search-paths: config-repo

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka